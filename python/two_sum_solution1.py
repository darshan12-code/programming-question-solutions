# Find two numbers in a list that add up to a specific target and return their indices.
# You are a chef in a chaotic kitchen. 
# You have a "Target Flavor Profile" of 10. 
# You have a shelf of ingredients, each with a numeric intensity. 
# You pick up a "5" (salt).
# Instead of searching the whole shelf for another "5,"
# you look at your "Quick-Note Ledger" to see if you already passed a "5" earlier.
# If you did, dinner is served!
# Example Input: nums = [2, 7, 11, 15],target = 9
# Example Output: [0, 1] (Since $2 + 7 = 9$)




# def twoSum(nums,target):
#     preMap={}

#     for i, n in enumerate(nums):
#         comp=target-n
#         if comp in preMap:
#             return [preMap[comp],i]
#         preMap[n]=i
    
# result= twoSum([2,2, 7, 11, 15],7)
# print(f"Output: {result}")







